# 🚂 Railway Environment Variables Example
# Copy this file to .env for local development

# Server Configuration
PORT=8080
NODE_ENV=development

# CORS Configuration
# For development: use * to allow all origins
# For production: specify exact domains separated by commas
CORS_ORIGIN=*

# Production Example:
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# Railway will automatically set these in production:
# PORT=10000 (or dynamic port)
# NODE_ENV=production
# CORS_ORIGIN=* (or your specific domains)
